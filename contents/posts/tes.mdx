---
slug: 'tes'
title: 'tes'
date: '2000-11-15'
excerpt: 'Visualize my journey through the scenic lands of Norway with rich, stunning pictures and daily travel logs.'
image: ''
---

javascript

```javascript
export async function getSlugs() {
  const suffix = '.mdx'
  const files = await readdir('contents/posts')
  return files
    .filter((file) => file.endsWith(suffix))
    .map((file) => file.slice(0, -suffix.length))
}
```

> pls reload if flowchart not loaded

```mermaid
%%{init: {'theme':'forest'}}%%
graph LR
 subgraph Tes

 A[Consumed] --> C[Literature]
 B[Come to mind] --> D[Fleeting]

 subgraph Capture
	 C[Literature]
	 D[Fleeting]
 end

 subgraph Connect
	 C --> E[Hub]
	 D --> E[Hub]
 end

 subgraph Crystallize
	 direction TB
	 E[Hub] --> F[Permanent]
 end

 F --> G[Insight & Ideas]
 end
```
